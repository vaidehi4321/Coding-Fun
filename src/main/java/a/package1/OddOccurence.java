package a.package1;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashMap;
import java.util.List;

public class OddOccurence {
    /*given a nonempty array with odd number of elements .Each element of the array can be paired with another element
    of the array with same value except for one element Find that element and return its value
     */
    public int findOddElementValue(int[] arr){
     return 0;
    }
}
